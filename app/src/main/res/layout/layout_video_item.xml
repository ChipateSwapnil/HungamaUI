<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/clMainVideo"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="5dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cvBucketVideoImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="0dp"
        android:padding="@dimen/bucket_music_card_padding"
        app:cardCornerRadius="5dp"
        app:layout_constraintBottom_toTopOf="@id/tvBucketVideoTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" >

        <ImageView
            android:id="@+id/ivBucketVideoImage"
            android:layout_width="@dimen/bucket_video_image_width"
            android:layout_height="@dimen/bucket_video_image_height" />
    </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/tvBucketVideoTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-black"
            android:maxLines="1"
            android:textColor="@color/black"
            android:textSize="@dimen/small_text"
            app:layout_constraintBottom_toTopOf="@id/tvBucketVideoSubTitle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/cvBucketVideoImage"
            app:layout_constraintTop_toBottomOf="@id/cvBucketVideoImage" />

        <TextView
            android:id="@+id/tvBucketVideoSubTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:textColor="@color/gray"
            android:textSize="@dimen/x_small_text"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/tvBucketVideoTitle"
            app:layout_constraintTop_toBottomOf="@id/tvBucketVideoTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>